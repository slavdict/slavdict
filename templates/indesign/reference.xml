<entry>
    <p aid:pstyle="FirstParagraph">

        <headword>{{ reference }}</headword>
        {{ space }}
        <i>см.<i>
        <csl>{{ entry.base_vars.0 }}</csl>
        {% if entry.homonym_order %}
            <homonym-number>{{ entry.homonym_order }}</homonym-number>
        {% endif %}

    </p>
</entry>

{# vim: set ft=django.xml: #}
