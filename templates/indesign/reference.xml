<entry>
    <p aid:pstyle="FirstParagraph" aid:cstyle="Text">

        <headword aid:cstyle="Headword">
            {{ reference|e }}
        </headword>{{ space }}
        <i aid:cstyle="Em">см.</i>{{ nbsp }}
        {% if entry.is_reference %}
            {% for x in entry.references %}
                {% if not loop.first %}
                    {{ backspace }},{{ space }}
                {% endif %}
                <csl aid:cstyle="CSLSegment">
                    {{ x.reference_ucs|e }}
                </csl>
                {% if x.homonym_order %}
                    <homonym-number aid:cstyle="HomonymNumber">
                        {{ x.homonym_order }}
                    </homonym-number>
                {% endif %}
            {% endfor %}
        {% else %}
            <csl aid:cstyle="CSLSegment">
                    {{ entry.base_vars.0.idem_ucs|e }}
            </csl>
            {% if entry.homonym_order %}
                <homonym-number aid:cstyle="HomonymNumber">
                    {{ entry.homonym_order }}
                </homonym-number>
            {% endif %}
        {% endif %}

    </p>
</entry>

{# vim: set ft=django.xml: #}
