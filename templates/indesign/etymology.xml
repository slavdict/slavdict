{% macro etymology(etym, isLanguageChanged, loop) %}


{% if isLanguageChanged %}
    {% if not loop.first %}
        {{ backspace }};{{ space }}
    {% endif %}

    <i>{{ etym.get_language_display() }}</i>&nbsp;
{% else %}
    {% if not loop.first %}
        {{ backspace }},{{ space }}
    {% endif %}
{% endif %}

{% if etym.unitext and etym.is_language('greek') %}
    <span>{{ etym.unitext }}</span>{{ space }}
{% elif etym.text %}
    <span class="{{ etym.get_language_css() }}"
        >{{ etym.text }}</span>{{ space }}
{% endif %}

{% if show_additional_info and etym.additional_info %}
    <span class="ai ai-etym">{{ etym.additional_info }}</span>
{% endif %}

{% endmacro %}

{# vim: set ft=django.xml: #}
